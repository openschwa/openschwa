# OS junk
.DS_Store
Thumbs.db

# Editors / IDEs
.idea/
.vscode/
*.swp

# Local env
.env
.env.*

# Python
__pycache__/
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
ui/dist/

# Desktop bundles (built locally; ~590 MB)
dist/

# Local voice recordings used for debugging. Never committed: they are personal
# audio, and this repo is public.
Samples/

# Eval corpora are never committed (see eval/README.md)
eval/data/

# Token-level checkpoints are corpus-derived intermediates; the md/json
# reports above them ARE committed (docs/architecture.md §2).
eval/reports/checkpoints/

# Workspace-local model cache (sandbox-friendly OPENSCHWA_MODEL_DIR)
.models/
.cache/
